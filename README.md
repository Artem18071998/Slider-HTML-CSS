# Slider-HTML-CSS

How to start project
--------------------

1. Install dependencies:

    `npm install`

2. Start server:

    - with **CMD**:

        `npm run start`

    - with **PowerShell**:

        `npm run start-ps1`
